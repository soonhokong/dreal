# if (ODE_ENABLED)
#   add_library(dsolvers nra_solver.cpp)
# else()
#   add_library(dsolvers nra_solver.cpp)
# endif()
add_library(dsolvers nra_solver.cpp)
target_link_libraries(dsolvers ${DREAL_LIBS} ${EXTRA_LIBS})
